{
    "builds": [
        {
            "src": "api/index.go",
            "use": "@vercel/go",
            "config": {
                "maxLambdaSize": "50mb"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/api/index.go"
        }
    ]
}